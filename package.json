{
  "name": "@xapi/xapi-demo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "archive": "npm run build && cd www && bestzip ../xapi-demo.zip *"
  },
  "devDependencies": {
    "@stencil/core": "^2.5.2",
    "bestzip": "^2.1.7"
  },
  "license": "MIT",
  "dependencies": {
    "@xapi/xapi": "^1.0.0"
  }
}
