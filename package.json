{
  "name": "@xapi/xapi-demo",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "archive": "npm run build && cd www && bestzip ../xapi-demo.zip *"
  },
  "devDependencies": {
    "@stencil/core": "^2.3.0",
    "bestzip": "^2.1.7"
  },
  "license": "MIT",
  "dependencies": {
    "@xapi/xapi": "^0.6.9"
  }
}
