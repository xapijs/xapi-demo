{
  "name": "@xapi/xapi-demo",
  "version": "2.1.2",
  "description": "",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "archive": "npm run build && cd www && bestzip ../xapi-demo.zip *"
  },
  "devDependencies": {
    "@stencil/core": "^4.2.0",
    "bestzip": "^2.2.1"
  },
  "license": "MIT",
  "dependencies": {
    "@xapi/xapi": "^2.1.2"
  }
}
